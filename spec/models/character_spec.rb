require 'rails_helper'

RSpec.describe Character, :type => :model do
  # it { should have_many :ability_scores }
  # it { should have_many(:abilities).through(:ability_scores) }
  # it { should have_one :character_race_association }
  # it { should have_one :race }
end
